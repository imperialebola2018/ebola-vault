Vault is now running in montagu-vault container.
To execute vault CLI commands use: `docker exec -it montagu-vault /bin/sh`
To initialise new vault run sh in container and then:
    ./init.sh
    vault unseal (multiple times until vault is unsealed)
    ./first-time-setup.sh [ROOT_TOKEN]
